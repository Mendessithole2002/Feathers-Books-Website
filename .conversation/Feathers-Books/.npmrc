auto-install-peers=false
strict-peer-dependencies=false
# Keep pnpm peer-dependency virtual-store directory names short enough for
# Windows CMake while preserving pnpm's real package resolution.
virtual-store-dir-max-length=32
